div.post
  h2 Write a new post
  label(for='title') Title: 
  input(ng-model='form.title', name='title')
  p Body:
  textarea(ng-model='form.text', cols='50', rows='15')
  button.editButton.editPost(ng-click='submitPost()') Submit Post